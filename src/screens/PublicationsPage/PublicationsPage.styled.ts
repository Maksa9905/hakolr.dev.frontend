'use client'

import { PublicationsFilters } from '@/widgets/publications'
import styled from 'styled-components'

export const StyledPublicationsFilters = styled(PublicationsFilters)`
  margin-bottom: 16px;
`

export const PopularPublicationSection = styled.section`
  margin-bottom: 24px;
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
`